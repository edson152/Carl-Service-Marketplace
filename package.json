{
  "name": "carl-service-marketplace",
  "version": "1.0.0",
  "description": "Carl Service Marketplace - Local service booking platform for Zimbabwe & South Africa",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n SERVER,CLIENT -c cyan,green \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
